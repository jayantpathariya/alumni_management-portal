import React from 'react';
import './heroImage.scss';

const HeroImage = () => {
  return (
    <div className="container">
      <div className="hero-text">
        Sometimes people graduate but they don't leave. They hang around for
        years, for no reason. I wanted think of ghosts like that, I decided.
      </div>
    </div>
  );
};

export default HeroImage;
